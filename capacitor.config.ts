import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'digital_bank',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
